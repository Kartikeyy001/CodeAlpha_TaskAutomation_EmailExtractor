# CodeAlpha_TaskAutomation_EmailExtractor
Smart Email Extractor is a Python GUI application built with Tkinter that automatically extracts email addresses from text files using Regular Expressions (Regex). The extracted emails are displayed in the application and saved to a separate text file, demonstrating automation, file handling, and pattern matching concepts.
